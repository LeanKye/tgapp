{
  "name": "tgapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@headlessui/react": "^2.2.7",
    "interactjs": "^1.10.27",
    "react-swipeable": "^7.0.2"
  }
}
